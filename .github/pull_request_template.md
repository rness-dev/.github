## Why

<!-- The problem this solves. Link the issue: "Closes #123". -->

## What changes

<!-- The behaviour a user or a reviewer will notice. -->

## Checklist

- [ ] `pnpm lint && pnpm format:check && pnpm typecheck && pnpm test && pnpm build && pnpm check:versions` pass
- [ ] A test proves the change (or the pull request says why none applies)
- [ ] Documentation updated where the behaviour changed
- [ ] No new runtime dependency — or the description explains why it is needed, pinned to an exact version
- [ ] No version bump (maintainers release)
